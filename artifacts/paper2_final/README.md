# Paper 2 Final Artifacts

**Scope:** `PAPER2`

This directory is reserved for frozen publication evidence for the communication-aware learned trajectory-prediction paper.

Final evidence may enter this directory only after the canonical data/provenance gate passes and the complete four-objective x five-seed learned archive is verified. Official held-out/OOD outputs must be generated only after development-only selection is frozen.

Required provenance includes source-data hashes, split manifest, processing commit/config, all 20 checkpoint/result identities, official evaluation population, downstream scheduler configuration, statistical unit and output hashes.

Current incomplete or development learned artifacts must not be silently promoted here.
